[
  {
    "name": "Accessing Elements I",
    "commands": [
      {
        "key": "a1.concat(a2)",
        "description": "Return new array by joining arrays a1 and a2 together"
      },
      {
        "key": "a1.join(separator)",
        "description": "Join all elements of array a1 into a string separated by separator arg"
      },
      {
        "key": "a1.slice(start,end)",
        "description": "Extract a section from start to end of array a1 and return a new array"
      },
      {
        "key": "a1.indexOf(obj)",
        "description": "Return first index of obj within array a1"
      },
      {
        "key": "a1.lastIndexOf(obj)",
        "description": "Return last index of obj within array a1"
      }
    ]
  },
  {
    "name": "Iterating I",
    "commands": [
      {
        "key": "a1.forEach(fn)",
        "description": "Calls function fn for each element in the array a1"
      },
      {
        "key": "a1.every(fn)",
        "description": "Return true if every element in array a1 satisfies provided testing function fn"
      },
      {
        "key": "a1.some(fn)",
        "description": "Return true if at least one element in array a1 satisfies provided testing function fn"
      },
      {
        "key": "a1.filter(fn)",
        "description": "Create a new array with all elements of array a1 which pass the filtering function fn"
      }
    ]
  },
  {
    "name": "Iterating II",
    "commands": [
      {
        "key": "a1.map(fn)",
        "description": "Create a new array with results of calling function fn on every element in array a1"
      },
      {
        "key": "a1.reduce(fn)",
        "description": "Apply a function fn against an accumulator and each value (from left to right) of the array as to reduce it to a single value"
      },
      {
        "key": "a1.reduceRight(fn)",
        "description": "Apply a function fn against an accumulator and each value (from right to left) of the array as to reduce it to a single value"
      }
    ]
  },
  {
    "name": "Mutating I",
    "commands": [
      {
        "key": "a1.pop()",
        "description": "Remove and return last element from array a1"
      },
      {
        "key": "a1.push(obj)",
        "description": "Add obj to end of array a1 and return new length"
      },
      {
        "key": "a1.reverse()",
        "description": "Reverse order of elements of array a1 in place"
      },
      {
        "key": "a1.sort()",
        "description": "Sort the elements of array a1 in place"
      },
      {
        "key": "a1.splice(start,deleteCount,items)",
        "description": "Change content of array a1 by removing existing elements and/or adding new elements"
      },
      {
        "key": "a1.unshift(obj)",
        "description": "Add obj to start of array a1 and return new length"
      }
    ]
  },
  {
    "name": "General I",
    "commands": [
      {
        "key": "a1.toString()",
        "description": "Return a string representing array a1 and its elements (same as a1. join(','))"
      },
      {
        "key": "a1.toLocaleString()",
        "description": "Return a localized string representing array a1 and its elements (similar to a1.join(','))"
      },
      {
        "key": "Array.isArray(var)",
        "description": "Return true if var is an array"
      },
      {
        "key": "a1.length",
        "description": "Return length of array a1"
      },
      {
        "key": "a1[0]",
        "description": "Access first element of array a1"
      }
    ]
  }
]